# DesktopSheet 사양서

**이 문서는 확정된 규칙과 아직 정하지 않은 항목을 갈라 적은 것입니다.** 1장부터 17장까지가 확정된 규칙이며 그대로 구현하면 됩니다. 부록 A는 그 규칙이 어느 질문에서 나왔는지를 적은 표입니다. 결정이 바뀌면 이 문서를 먼저 고치고 코드를 고칩니다.

---

## 0. 이 프로그램은 스티커노트처럼 쓰는 데스크톱 스프레드시트입니다

바탕화면에 늘 띄워 두고 숫자를 적고 계산하는 것이 용도입니다. 이 용도가 나머지 규칙을 거의 다 정합니다 — 창이 작아야 하므로 셀 폭이 고정이고, 셀 폭이 고정이므로 한 칸에 보일 글자 수가 상수가 되며, 그 상수가 숫자를 언제 지수로 바꿀지까지 정합니다.

만들려는 기능은 다음과 같습니다.

- 셀 참조와 사칙연산, 제곱, 로그 계산
- 시트 탭 여러 장
- 셀 음영과 글자색 각 10가지
- 엑셀과 같은 표기법의 숫자 서식
- 고정폭 글꼴, 고정된 글자 크기와 셀 폭
- 드래그로 다중 셀 선택, Ctrl+C / Ctrl+V
- 작업 표시줄에 뜨지 않고 트레이 아이콘으로만 부름
- 프레임리스 창
- 저장 절차 없이 자동 보존
- 버벅거리지 않는 반응 속도

---

## 1. 표시 예산은 10자이고 셀 폭은 13자입니다

셀 하나의 가로 폭은 13자이며, 그 안을 세 몫으로 나눠 씁니다. 아래 표는 셀 한 칸의 가로 폭을 무엇이 얼마나 차지하는지를 적은 것입니다.

| 몫 | 자릿수 | 설명 |
|---|---|---|
| 좌우 공백 | 2 | 양쪽 각 1자, 항상 비워 둠 |
| 부호 | 1 | 음수일 때만 씀 |
| 내용 | 10 | 숫자·날짜·텍스트가 쓰는 예산 |

**모든 표시는 내용 10자 안에서 끝납니다.** 예외를 두지 않습니다 — 10자를 넘기면 줄이거나(반올림), 바꾸거나(지수 표기), 덮거나(`#` 채움), 자릅니다(`…`). 셀 폭을 넓히는 선택지는 없으므로, 내용이 예산을 넘겼을 때 원본을 보는 수단은 툴팁 하나입니다(9장).

글꼴은 나눔고딕코딩 12pt입니다. 12pt는 96 DPI에서 12 x 96/72 = 16px이고, 이 글꼴의 영문 한 자는 em의 절반인 16 x 0.5 = 8px, 한글 한 자는 영문 두 자 폭인 16px입니다. 따라서 셀 폭은 13 x 8 = 104px, 행 높이는 20px 안팎입니다. 한글은 한 자가 두 자리를 먹으므로 10자 예산에 한글은 5자만 들어갑니다.

글꼴 실측으로 확정할 가정이 둘 있습니다 — 영문 한 자가 정확히 em의 절반인지, 말줄임표 `…`(U+2026)가 한글과 같은 두 자리 폭인지입니다. 실측 값이 다르면 셀 폭 픽셀과 6장의 자르기 규칙을 그 값으로 다시 계산합니다.

---

## 2. 숫자는 10자에 들어가면 그대로 쓰고 넘치면 지수로 바꿉니다

**판정 기준은 값의 크기가 아니라 표시 문자 수입니다.** 반올림을 마친 뒤의 문자 수가 10자를 넘으면 지수 표기로 바꿉니다. 값 경계(1억, 1e-6 따위)를 따로 두지 않으므로 규칙이 한 줄로 유지됩니다.

이 규칙에서 나오는 경계는 다음과 같습니다.

- 정수부는 10자리까지 일반 표기입니다. 9999999999(10자)는 그대로 쓰고, 10000000000 = 1e10(11자)부터 지수로 갑니다.
- 소수는 0.00000001 = 1e-8(10자)까지 일반 표기입니다. 0.000000001 = 1e-9(11자)부터 지수로 갑니다. 소수점 이하 0이 여덟 개부터 지수라는 뜻입니다.
- 소수 자리는 10자에 맞춰 반올림합니다. 1234.5678901(12자)은 `1234.56789`(10자)로 씁니다.
- 반올림한 결과로 판정합니다. 9999999999.6은 반올림하면 10000000000(11자)이므로 지수로 갑니다.

일반 서식에는 천 단위 콤마를 넣지 않습니다. 콤마를 넣으면 1,000,000이 9자, 99,999,999가 10자가 되어 예산을 훨씬 일찍 먹어 치우기 때문입니다. 콤마는 사용자가 `#,##0` 서식을 직접 지정한 셀에만 붙습니다(4장).

부호는 내용 예산 밖의 자리를 씁니다. `-9999999999`는 부호 1자 + 내용 10자 = 11자이고, 좌우 공백 2자를 더해 셀 폭 13자에 정확히 들어갑니다.

숫자는 오른쪽 정렬입니다(오른쪽 공백 1자 안쪽에 붙임).

---

## 3. 지수 표기는 엑셀 표기법으로 10자를 채웁니다

엑셀의 `0.0000E+00` 모양을 그대로 씁니다. `E`는 대문자이고 지수의 부호는 양수일 때도 `+`를 적습니다.

- 지수가 두 자리일 때는 유효숫자 5자리입니다: `1.2345E+08`(10자), `1.2345E-08`(10자).
- 지수가 세 자리일 때는 유효숫자 4자리입니다: `1.000E+100`(10자).
- 음수는 부호 자리를 따로 쓰므로 `-1.2345E+08`(11자)이 되고 셀 폭 13자 안에 들어갑니다.

유효숫자를 5자리로 줄이면 원래 값이 화면에서 사라집니다 — 123456789012는 `1.2346E+11`로 보이고, 반올림으로 생기는 상대 오차는 0.00005/1.2346 = 0.004%입니다. 원본 값은 툴팁으로 확인합니다(9장).

---

## 4. 사용자가 지정한 서식은 그 서식대로 쓰고 넘치면 `#`으로 덮습니다

서식을 지정한 셀에서는 2장의 자동 지수 전환이 일어나지 않습니다. 지정한 서식으로 만든 문자열이 10자를 넘으면 지수로 바꾸지 않고 `##########`(10자)으로 덮습니다. 값을 보려면 툴팁을 씁니다.

`#,##0` 서식의 표시 한계는 여기서 나옵니다 — 99,999,999(10자)까지는 보이고 999,999,999(11자)부터 `#`으로 덮입니다.

서식 코드는 엑셀 표기법을 따르되 다음 범위만 해석합니다.

| 구분 | 지원 | 예 |
|---|---|---|
| 자리표시자 | 지원 | `0`, `#`, `.`, `,` |
| 퍼센트 | 지원 | `0.00%` |
| 지수 | 지원 | `0.0000E+00` |
| 날짜 | 지원 | `yyyy`, `mm`, `dd` |
| 구역 구분 | 지원 | `0.00;[Red]-0.00;-` (양수;음수;0) |
| 색 지정 | 지원 | `[Red]` 등 10색(11장) |
| 분수 | 제외 | `# ?/?` |
| 조건 구역 | 제외 | `[>100]` |
| 통화 기호 자동 변환 | 제외 | `₩#,##0` |
| 텍스트 구역 | 제외 | `@` |

구역 구분을 지원하므로 음수를 빨갛게 찍고 0을 대시로 비우는 쓰임이 됩니다.

---

## 5. 날짜는 엑셀식 일련번호로 저장합니다

1900-01-01을 1로 세는 정수로 저장하고 서식만 날짜로 씌웁니다. 이렇게 하면 날짜 산술이 따라옵니다 — `=B1-A1`은 두 날짜 사이의 일수이고, `=A1+30`은 30일 뒤입니다.

표시 기본 서식은 `yyyy-mm-dd`입니다. 10자라 예산에 정확히 맞습니다.

입력으로 날짜를 알아보는 형식은 셋입니다. 연도를 반드시 적어야 하며, 연도를 생략한 `9/11` 같은 입력은 날짜로 보지 않고 텍스트로 둡니다.

- `2026-09-11`
- `2026.9.11`
- `26-9-11`

시간 서식은 넣지 않습니다. `yyyy-mm-dd hh:mm`은 16자로 10자 예산을 넘고, 한 칸에 날짜와 시간을 같이 담을 수 없어 규칙이 갈라지기 때문입니다.

---

## 6. 텍스트는 셀 안에 가두고 넘치면 자릅니다

오른쪽 칸이 비어 있어도 흘려 쓰지 않습니다. 내용이 10폭을 넘으면 앞에서부터 8폭까지만 남기고 잘림 표시 `…`를 붙입니다. 길이는 글자 수가 아니라 글꼴에서 차지하는 자리로 세며, 한글 한 자가 2폭, 영문 한 자가 1폭, `…`가 2폭입니다. 남길 폭 8은 예산 10폭에서 잘림 표시 2폭을 뺀 값이고, 실측에서 `…`가 1폭으로 그려지면 남길 폭을 9로 바꿉니다(1장).

8폭 경계가 한글 한 자의 가운데에 걸리면 그 글자를 통째로 버립니다. 그래서 잘린 결과가 10폭이 아니라 9폭이 되기도 합니다.

- `합계금액표`(한글 5자 = 10폭)는 넘치지 않아 그대로 보입니다.
- `합계금액표시`(12폭)는 `합계금액…`(8 + 2 = 10폭)으로 잘립니다.
- `ABCDEFGHIJK`(11폭)는 `ABCDEFGH…`(8 + 2 = 10폭)로 잘립니다.
- `A합계금액표`(11폭)는 `A합계금…`(7 + 2 = 9폭)으로 잘립니다 — 네 번째 한글까지 넣으면 9폭이 되어 남길 폭 8을 넘기 때문입니다.

텍스트는 왼쪽 정렬입니다(왼쪽 공백 1자 안쪽에 붙임).

---

## 7. 입력은 네 가지만 자동으로 알아듣고 나머지는 텍스트로 둡니다

타이핑만으로 서식이 정해지도록 아래 네 가지를 자동 인식합니다. 서식 메뉴를 열지 않아도 되게 하려는 것입니다.

| 입력 | 저장되는 값 | 붙는 서식 |
|---|---|---|
| `1234.5` | 1234.5 | 일반 |
| `50%` | 0.5 | 퍼센트 |
| `1.2e5` | 120000 | 일반 |
| `2026-09-11` | 46276 (일련번호) | `yyyy-mm-dd` |

콤마가 든 입력은 자동 인식하지 않습니다. `1,234`를 치면 값 1234가 아니라 텍스트 `1,234`가 됩니다. 콤마를 쓰려면 셀에 `#,##0` 서식을 직접 지정합니다.

`=`로 시작하면 수식입니다. 위 넷과 수식에 해당하지 않는 입력은 모두 텍스트입니다.

---

## 8. 수식과 계산

### 8.1 수식은 `=`로 시작하고 연산자는 엑셀과 같은 순서로 묶입니다

쓸 수 있는 연산자는 사칙연산 `+ - * /`, 제곱 `^`, 후위 백분율 `%`, 괄호, 그리고 비교 연산자 `> < >= <= = <>` 입니다. `=A1*10%`는 A1의 10%입니다.

아래 표는 위에 있는 것을 먼저 묶는다는 뜻으로 읽습니다. 순서는 엑셀과 같습니다.

| 순위 | 연산자 |
|---|---|
| 1 | 괄호 `( )` |
| 2 | 범위 `:` |
| 3 | 단항 마이너스 `-` |
| 4 | 백분율 `%` |
| 5 | 제곱 `^` (왼쪽부터 묶음) |
| 6 | `*` `/` |
| 7 | `+` `-` |
| 8 | 비교 `> < >= <= = <>` |

단항 마이너스가 제곱보다 위에 있고 제곱을 왼쪽부터 묶는 것이 수학 관례와 다릅니다. 엑셀에서 쓰던 식을 붙여넣었을 때 답이 달라지지 않게 하려고 엑셀 쪽을 따릅니다.

| 식 | 이 프로그램과 엑셀 | 수학 관례 |
|---|---|---|
| `=-2^2` | 4 | -4 |
| `=2^3^2` | 64 | 512 |

### 8.2 함수는 스무 개입니다

| 갈래 | 함수 |
|---|---|
| 집계 | `SUM`, `AVERAGE`, `MIN`, `MAX`, `COUNT` |
| 반올림·정수 | `ROUND`, `ROUNDUP`, `ROUNDDOWN`, `INT`, `MOD` |
| 수학 | `ABS`, `SQRT`, `EXP`, `POWER` |
| 로그 | `LOG(x,[밑])`, `LN`, `LOG10` |
| 조건 | `IF(조건, 참일값, [거짓일값])` |
| 날짜 | `TODAY()`, `DATE(연,월,일)` |

`LOG`의 밑은 기본이 10입니다. 엑셀과 같으므로 `=LOG(100)`은 2이고, 자연로그는 `LN`으로 씁니다. `POWER(a,b)`는 `a^b`와 같으며 엑셀 식을 그대로 붙여넣을 수 있게 두었습니다.

집계 함수는 범위 안의 텍스트와 빈 칸을 무시합니다. 합계 열에 `합계` 같은 라벨이 섞여 있어도 `SUM`이 동작합니다. `AVERAGE`의 분모도 숫자가 든 칸만 셉니다 — 빈 칸이 섞이면 0으로 세는 것이 아니라 아예 빼고 나눕니다. `COUNT`는 숫자가 든 칸만 셉니다.

`IF`의 세 번째 인수를 생략하면 조건이 거짓일 때 `FALSE`가 됩니다.

문자열 함수(`LEFT`, `LEN`, `CONCAT`)와 연결 연산자 `&`는 넣지 않습니다. 텍스트를 10폭에 가두므로 문자열을 조립해 봐야 잘리기 때문입니다.

### 8.3 참조는 범위·절대·시트 간 셋을 지원합니다

- 범위는 `A1:B3`이며 함수의 인수 자리에서만 씁니다. `=A1:B3`처럼 단독으로 쓰면 `#VALUE!`입니다.
- 절대 참조는 `$A$1`이고 혼합 참조 `$A1`, `A$1`도 씁니다. 붙여넣을 때 참조를 미는 규칙은 10.1에 있습니다.
- 시트 간 참조는 `Sheet2!A1`입니다. 시트 이름에 공백이 있으면 `'내 시트'!A1`처럼 작은따옴표로 묶습니다.
- 빈 칸을 참조하면 0입니다. `=A1+1`에서 A1이 비어 있으면 1입니다.
- 함수 이름과 셀 주소는 입력받은 뒤 대문자로 정규화하고 공백을 지웁니다. `sum( a1 : b2 )`는 `SUM(A1:B2)`로 저장됩니다.
- 인수 구분자는 쉼표입니다.

### 8.4 값은 세 종류이고 오류는 엑셀 이름을 씁니다

값의 종류는 숫자(날짜 포함), 텍스트, 참·거짓 셋입니다. 참·거짓은 화면에 `TRUE`와 `FALSE`로 찍고 계산에 쓰일 때는 1과 0으로 셉니다. 종류가 다른 값을 비교하면 숫자가 텍스트보다 항상 작습니다(엑셀과 같음).

아래 표는 어떤 셀에 어떤 오류가 찍히는지를 적은 것입니다. 가장 긴 `#DIV/0!`이 7자라 표시 예산 10자 안에 모두 들어갑니다.

| 오류 | 나는 때 |
|---|---|
| `#DIV/0!` | 0으로 나눔 |
| `#VALUE!` | 텍스트에 숫자 연산을 검, 범위를 단독으로 씀 |
| `#NAME?` | 모르는 함수 이름 |
| `#REF!` | 참조하던 시트가 사라짐 |
| `#NUM!` | 로그의 진수가 0 이하, 음수의 제곱근 |
| `#N/A` | 값이 없음 |
| `#CIRC!` | 순환 참조 |

순환 참조는 엑셀처럼 경고 대화상자를 띄우지 않습니다. 늘 띄워 두는 창에 대화상자가 끼어드는 것이 거슬리기 때문입니다. 순환에 낀 셀에 `#CIRC!`를 찍고 그대로 둡니다. 엑셀에는 없는 오류 이름이라 이 프로그램에서 새로 만든 것입니다.

### 8.5 계산은 원본 값으로 하고 반올림은 표시할 때만 합니다

셀에 보이는 것은 반올림한 결과이고, 계산에 쓰는 것은 반올림하기 전의 값입니다. 표시 반올림은 엑셀과 같은 사사오입(0.5를 절댓값이 커지는 쪽으로)입니다. .NET의 기본값인 은행가 반올림이 끼어들지 않도록 반올림 방식을 명시적으로 지정합니다.

더하기와 빼기의 결과가 피연산자에 견주어 아주 작으면 0으로 맞춥니다. 엑셀과 같은 처리이며, 이것이 없으면 `=0.1+0.2-0.3`이 이진 부동소수점의 찌꺼기인 5.551115123125783e-17을 남기고, 그 값이 2장의 지수 규칙에 걸려 `5.5511E-17`로 화면에 뜹니다. 이 처리를 넣으면 `0`으로 보입니다.

값 0은 빈칸으로 두지 않고 `0`으로 찍습니다.

### 8.6 재계산은 바뀐 셀에 달린 것만 다시 셉니다

셀 사이의 참조를 의존 그래프로 들고 있다가, 값이 바뀐 셀과 그 뒤에 달린 셀만 다시 계산합니다. 수동 계산 모드는 두지 않습니다.

수식 편집은 F2 또는 더블클릭으로 들어갑니다. 편집 중에 다른 셀을 클릭하면 그 주소가 식에 들어갑니다. 셀에는 계산 결과가 보이고 수식 원문은 툴팁으로 봅니다(9장).

---

## 9. 원본을 보는 수단은 마우스를 올렸을 때 뜨는 박스 툴팁입니다

셀 폭을 넓힐 수 없으므로 반올림·지수·`#`·잘린 텍스트는 화면에서 원본을 잃습니다. 마우스를 셀 위에 올리면 박스 툴팁이 떠서 원본을 그대로 보여줍니다.

툴팁에 담는 것은 둘입니다.

- 저장된 값 전문(반올림·지수·`#`·잘림을 거치지 않은 값)
- 수식 셀이면 수식 원문

표시가 원본과 다르지 않은 셀에는 툴팁을 띄우지 않습니다.

---

## 10. 편집

### 10.1 서식은 셀에 붙박여 있고 복사·붙여넣기는 값과 수식만 옮깁니다

음영·글자색·숫자 서식은 그 자리의 셀에 속한 것이지 내용에 딸린 것이 아닙니다. 표를 통째로 옮겨도 색은 원래 자리에 남고, 붙여넣은 자리에는 그 칸이 원래 가지고 있던 서식이 그대로 적용됩니다. 서식만 따로 옮기는 수단(엑셀의 서식 복사 붓)은 두지 않습니다 — 10색을 Ctrl+1~0으로 바로 칠할 수 있으므로 다시 칠하는 편이 빠릅니다.

| 키 | 하는 일 |
|---|---|
| Ctrl+C | 선택 영역의 값과 수식을 클립보드에 담음 |
| Ctrl+V | 값과 수식을 붙임. 수식의 상대 참조는 밀림 |
| Ctrl+Shift+V | 수식을 계산 결과 값으로 굳혀 붙임 |
| Ctrl+X | 복사한 뒤 원본의 값과 수식을 지움. 원본 칸의 서식은 남음 |
| Delete | 선택 영역의 값과 수식을 지움. 서식은 남음 |

수식의 참조는 엑셀처럼 밉니다. `=A1+B1`을 한 칸 아래에 붙이면 `=A2+B2`가 되고 `$A$1`은 고정됩니다. 8.3에서 절대 참조를 넣기로 한 것이 이 이동을 전제로 한 것이라, 이동을 빼면 `$`가 뜻을 잃습니다. 밀린 참조가 시트 밖으로 나가면 `#REF!`를 찍습니다.

잘라내기는 옮겨 간 칸을 가리키도록 남의 수식을 고쳐 주지 않습니다. 엑셀은 이 참조 따라가기를 해 주는데, 규칙이 복잡한 데 비해 이 프로그램에서 득이 적습니다.

붙여넣는 영역의 크기는 이렇게 맞춥니다.

- 한 칸을 복사해 여러 칸에 붙이면 선택한 영역을 그 값으로 채웁니다.
- 여러 칸을 복사해 한 칸을 고르고 붙이면 그 칸을 왼쪽 위로 삼아 펼칩니다.
- 엑셀의 배수 반복 채우기(2칸을 복사해 6칸에 붙이면 세 번 반복)는 넣지 않습니다.
- 시트 경계를 넘는 만큼은 버립니다.

### 10.2 클립보드는 탭 구분 텍스트로만 주고받습니다

내보낼 때는 탭 구분 텍스트 하나만 싣습니다. 엑셀·메모장·웹 어디에나 붙고, 색과 서식은 싣지 않습니다. 색을 옮기려면 HTML 클립보드 포맷과 색 대응표가 필요한데, 색은 이 프로그램 안에서 구분하려고 쓰는 것이지 밖으로 넘길 물건이 아니라고 보았습니다.

받을 때는 탭을 열, 줄바꿈을 행으로 나눕니다. 들어온 문자열에는 7장의 입력 인식 규칙을 그대로 적용하되, **붙여넣기에 한해 콤마가 든 숫자를 숫자로 읽습니다**. 엑셀에서 복사하면 화면에 보이던 문자열이 실려 오므로 `#,##0` 서식이 걸린 칸은 `1,234`로 옵니다. 이 예외가 없으면 엑셀에서 가져온 금액이 전부 텍스트가 되어 `SUM`에서 빠집니다.

엑셀 쪽 날짜 표시가 `2026-09-11`이면 날짜로 읽히고, `2026년 9월 11일`이면 5장의 입력 형식 셋에 없어 텍스트가 됩니다.

셀을 편집하는 중(F2)에 붙여넣으면 그 칸 안의 글자로 들어갑니다.

복사한 영역에는 점선 테두리를 그리고 Esc로 지웁니다. 점선이 계속 움직이는 애니메이션은 넣지 않습니다 — 늘 띄워 두는 창에서 거슬리고, 매 프레임 다시 그리는 비용이 붙습니다.

### 10.3 되돌리기는 20단계입니다

Ctrl+Z로 되돌리고 Ctrl+Y로 다시 실행하며, 20단계를 기억합니다. 저장 절차가 없어 "저장하지 않고 닫기"라는 탈출구가 없으므로, 되돌리기가 실수를 되돌리는 유일한 수단입니다. 붙여넣기 한 번과 지우기 한 번이 각각 한 단계를 씁니다.

---

## 11. 표시 규칙을 이 표로 검증합니다

아래 표는 구현 뒤 그대로 시험할 수 있도록 입력 값과 서식에 대해 화면에 보여야 할 문자열을 적은 것입니다. 문자 수는 부호를 포함한 값이며, 셀 폭 13자에서 좌우 공백 2자를 뺀 11자를 넘지 않아야 합니다.

| 입력 값 | 서식 | 표시 | 문자 수 | 근거 |
|---|---|---|---|---|
| 1234.5 | 일반 | `1234.5` | 6 | 예산 안 |
| 1234.5678901 | 일반 | `1234.56789` | 10 | 10자로 반올림 |
| 99999999 | 일반 | `99999999` | 8 | 예산 안 |
| 100000000 | 일반 | `100000000` | 9 | 1억도 일반 표기 |
| 9999999999 | 일반 | `9999999999` | 10 | 정수부 상한 |
| 10000000000 | 일반 | `1.0000E+10` | 10 | 11자라 지수 전환 |
| 9999999999.6 | 일반 | `1.0000E+10` | 10 | 반올림 후 판정 |
| 123456789012 | 일반 | `1.2346E+11` | 10 | 지수, 유효숫자 5 |
| 1e100 | 일반 | `1.000E+100` | 10 | 지수 3자리, 유효숫자 4 |
| -9999999999 | 일반 | `-9999999999` | 11 | 부호는 예산 밖 |
| 0.000001 | 일반 | `0.000001` | 8 | 예산 안 |
| 0.00000001 | 일반 | `0.00000001` | 10 | 소수 하한 |
| 0.000000001 | 일반 | `1.0000E-09` | 10 | 11자라 지수 전환 |
| 0.1+0.2-0.3 | 일반 | `0` | 1 | 0으로 맞춤 |
| 99999999 | `#,##0` | `99,999,999` | 10 | 콤마 서식 상한 |
| 999999999 | `#,##0` | `##########` | 10 | 11자라 `#` 채움 |
| 0.5 | `0.00%` | `50.00%` | 6 | 예산 안 |
| 2026-09-11 | `yyyy-mm-dd` | `2026-09-11` | 10 | 예산에 딱 맞음 |
| `합계금액표시` | 텍스트 | `합계금액…` | 8폭 + 잘림 표시 | 한글 2폭으로 세어 8폭까지 남김 |

## 12. 그리드와 시트 탭

### 12.1 시트 한 장은 200행 x 26열입니다

한 장에 200 x 26 = 5,200칸이고 다섯 장이면 26,000칸입니다. 크기를 고정했으므로 화면에 보이는 칸만 그리면 되고, 스크롤 위치에 따라 그릴 범위를 계산하는 가상화 코드가 필요 없습니다. 열 머리글은 A부터 Z까지입니다.

행과 열을 삽입하거나 삭제하는 기능은 넣지 않습니다(부록 B). 넣으면 삽입·삭제할 때마다 모든 시트의 모든 수식을 훑어 참조를 옮기고, 지워진 칸을 가리키던 식을 `#REF!`로 바꾸는 처리가 따라붙습니다.

### 12.2 시트는 다섯 장까지 두고 탭은 위쪽에 붙입니다

시트는 추가·삭제·이름 변경을 할 수 있으며 다섯 장이 상한입니다. 탭은 창 위쪽에 붙이고 탭 오른쪽의 빈 자리를 창 끌기 영역으로 겸합니다. 프레임리스 창이라 제목 표시줄이 없는데, 탭을 엑셀처럼 아래에 두면 창을 끌 자리를 위쪽에 20px 따로 만들어야 하고 그만큼 세로가 늘어납니다.

### 12.3 창은 5열 20행이 보이는 560 x 444px로 엽니다

창 크기는 1장에서 정한 셀 폭 104px과 행 높이 20px에서 그대로 나옵니다.

| 자리 | 크기 | 계산 |
|---|---|---|
| 그리드 가로 | 520px | 5열 x 104px |
| 행 머리글 | 40px | 행 번호 최대 3자리 x 8px + 좌우 여백 각 8px |
| **창 가로** | **560px** | 520 + 40 |
| 그리드 세로 | 400px | 20행 x 20px |
| 열 머리글 | 20px | 행 높이와 같게 |
| 시트 탭 | 24px | |
| **창 세로** | **444px** | 400 + 20 + 24 |

시트는 26열 200행인데 창에는 5열 20행만 보이므로 나머지는 스크롤로 봅니다. 스크롤바는 8px 폭으로 그리드 위에 겹쳐 그리고 창 크기 계산에는 넣지 않습니다 — 자리를 차지하게 두면 창 가로가 560px에서 568px로 늘어나고, 그만큼 스티커노트가 커집니다. 마우스 휠은 세로로 3행, Shift+휠은 가로로 3열씩 움직입니다. 방향키로 커서가 보이는 범위를 벗어나면 화면이 따라갑니다.

### 12.4 창 크기는 마우스로 조절합니다

글자 크기와 셀 폭이 고정이므로 창을 늘리면 보이는 칸 수만 늘어납니다. 프레임리스라 테두리가 없어서 가장자리 6px을 크기 조절로 잡히는 자리로 둡니다 — 이 값이 작으면 잡기 어렵고 크면 셀을 클릭하려다 창이 늘어납니다.

최소 크기는 3열 5행이 보이는 352 x 144px입니다. 최대는 시트 전체가 보이는 크기(26열 200행)를 넘지 않습니다. 창 나머지 동작(부르는 단축키, 항상 위, 작업 표시줄 숨김)은 A-5에서 정합니다.

---

## 13. 자동 저장

### 13.1 저장 파일은 `%APPDATA%\DesktopSheet\book.json` 하나입니다

다섯 시트를 한 파일에 담고 값이 든 칸만 적습니다. 1,000칸을 쓴 파일이 100KB 남짓이고 26,000칸을 꽉 채워도 3MB 안쪽이라, 통째로 다시 써도 1ms가 걸리지 않습니다. JSON이라 메모장으로 열어 읽을 수 있고 파일 하나만 복사하면 다른 PC로 옮겨집니다.

파일에 담는 것은 셀의 값·수식·서식, 시트의 이름과 순서, 그리고 창의 위치·크기와 마지막 선택 위치입니다(창 상태를 복원할지는 A-5에서 확정).

### 13.2 마지막 입력에서 500ms 뒤에 쓰고, 계속 치면 5초마다 한 번은 씁니다

키를 누를 때마다 쓰면 디스크가 쉬지 않고, 유휴만 기다리면 계속 타이핑하는 동안 한 번도 쓰이지 않습니다. 그래서 둘을 겹쳐 씁니다 — 마지막 입력에서 500ms가 지나면 쓰고, 그 사이 계속 치고 있더라도 마지막으로 쓴 지 5초가 지나면 한 번 씁니다.

쓰기는 배경 스레드에서 합니다. UI 스레드에서 동기로 쓰면 파일이 커질수록 타이핑이 끊깁니다.

윈도우가 꺼지거나 로그오프할 때(WM_QUERYENDSESSION), 그리고 트레이 메뉴로 종료할 때 한 번 더 씁니다.

### 13.3 쓰기는 임시 파일에 하고 이름을 바꿔 넣습니다

`book.tmp`에 전부 쓴 뒤 이름을 바꿔 `book.json` 자리에 넣고, 밀려난 기존 파일은 `book.prev.json`으로 남깁니다. 윈도우의 파일 교체 호출 하나가 이 셋을 한 번에 하므로 반쯤 쓰이다 만 파일이 남지 않고, `book.prev.json`은 그 호출이 덤으로 만들어 주는 것이라 비용이 없습니다.

날짜별 스냅샷 이력은 두지 않습니다. **`book.prev.json`은 파일이 깨졌을 때 쓰는 것이지 사람의 실수를 되돌리는 수단이 아닙니다** — 500ms 뒤면 실수한 상태가 `book.json`에, 그 직전 상태가 `book.prev.json`에 들어가므로 둘 다 실수한 뒤의 파일입니다. 실수를 되돌리는 수단은 되돌리기 20단계뿐이고, 그것도 프로그램을 껐다 켜면 사라집니다.

### 13.4 파일을 못 읽으면 직전 복사본으로 되살리고 트레이 알림만 띄웁니다

`book.json`을 읽지 못하면 `book.prev.json`으로 되살리고 트레이에 알림 하나를 띄웁니다. 어느 것으로 되살릴지 묻는 창은 띄우지 않습니다 — 늘 띄워 두는 프로그램이 시작할 때마다 묻는 것은 성가시고, 알림으로 되살린 사실만 알면 나머지는 저장 폴더를 열어 직접 처리할 수 있습니다.

둘 다 읽지 못하면 빈 시트로 시작하고, 읽지 못한 파일은 `book.broken-20260911-1423.json`처럼 시각을 붙여 옆에 남깁니다.

### 13.5 내보내기는 그리드 우클릭 메뉴 두 줄입니다

- **이 시트를 CSV로 내보내기** — 보고 있는 시트 한 장을 내보냅니다.
- **저장 폴더 열기** — 탐색기로 `%APPDATA%\DesktopSheet`를 엽니다. 백업이나 다른 PC로 옮기기가 이걸로 해결됩니다.

CSV에는 화면에 보이는 문자열이 아니라 저장된 값을 씁니다. `1.2345E+08`이나 `##########`을 쓰면 CSV가 쓸모없어지기 때문입니다. 수식은 계산 결과를 쓰고, 날짜는 일련번호가 아니라 `2026-09-11`로 씁니다. 쉼표·따옴표·줄바꿈이 든 텍스트는 따옴표로 묶습니다. 인코딩은 BOM을 붙인 UTF-8입니다 — BOM이 없으면 엑셀이 한글을 깨뜨려 읽습니다.

가져오기는 넣지 않습니다. 붙여넣기(10.2)로 대신합니다.

---

## 14. 창과 트레이

### 14.1 창은 하나뿐이고 창에는 단추가 없습니다

여러 장을 띄우는 스티커 메모와 달리 이 프로그램의 창은 하나로 고정입니다. 나눠 적을 일은 시트 탭이 맡습니다. 두 번 실행하면 새 창을 띄우지 않고 이미 떠 있는 창을 앞으로 가져옵니다.

닫기 단추와 최소화 단추를 만들지 않습니다. 창을 치우고 싶으면 다른 창으로 덮으면 됩니다. 종료는 트레이 아이콘의 우클릭 메뉴에만 두므로 실수로 끄는 일이 없습니다.

### 14.2 창은 일반 창처럼 덮이고 덮습니다

항상 위로 띄우지 않습니다. 클릭한 순서대로 다른 창에 덮이고 다른 창을 덮습니다. 포커스를 잃어도 숨지 않으므로, 값을 보면서 다른 프로그램에 옮겨 적는 동안 창이 그대로 남아 있습니다.

작업 표시줄에 뜨지 않고 Alt+Tab 목록에도 넣지 않습니다(WS_EX_TOOLWINDOW 확장 스타일). **그래서 창이 다른 창에 덮였을 때 되찾는 길은 트레이 아이콘 클릭 하나뿐입니다.** 전역 단축키는 두지 않습니다 — 어느 키를 잡아도 다른 프로그램에서 그 키를 쓰던 사람에게는 빼앗는 것이 됩니다.

### 14.3 트레이 아이콘이 창을 부르는 유일한 길입니다

- **왼쪽 클릭** — 창을 보이게 하고 맨 앞으로 가져옵니다.
- **우클릭 메뉴** — 창 숨기기, 로그온할 때 자동 시작(체크), 종료.

창 숨기기를 메뉴에 둔 것은 창에 닫기 단추가 없기 때문입니다. 숨긴 창은 트레이 아이콘을 클릭해 다시 부릅니다.

파일을 되살렸을 때(13.4) 띄우는 알림도 이 아이콘에 붙습니다.

### 14.4 로그온할 때 자동으로 뜨되 창은 띄우지 않습니다

레지스트리의 실행 항목에 등록해 로그온할 때 시작합니다. 이때는 트레이 아이콘만 올라오고 창은 뜨지 않습니다 — 부팅 직후에 560 x 444px짜리 창이 화면을 차지하고 있을 이유가 없습니다. 직접 실행하면 창이 뜹니다. 자동 시작은 트레이 메뉴에서 끄고 켭니다.

### 14.5 DPI 배율을 따라갑니다

150% 배율을 쓰는 화면에서는 모든 치수에 1.5를 곱합니다 — 셀 폭 104px이 156px, 창 560 x 444px이 840 x 666px이 되어 화면에서 차지하는 크기가 100% 화면과 같아집니다.

"글자 크기와 셀 폭 고정"은 창을 늘려도 글자가 커지지 않는다는 뜻이지, 모니터가 달라지면 물리적으로 작아진다는 뜻이 아닙니다. 배율이 다른 모니터 두 대를 쓰면 창을 옮길 때 그 모니터의 배율로 다시 그립니다(Per-Monitor DPI v2).

### 14.6 종료할 때의 창 위치와 선택을 되살립니다

창 위치·크기, 보고 있던 시트, 선택한 칸을 저장해 다음 실행에서 되살립니다(13.1). 저장된 위치가 화면 밖이면 — 노트북에서 외부 모니터를 뺐을 때가 그렇습니다 — 주 모니터 안으로 끌어들여 띄웁니다.

---

## 15. 서식 적용

### 15.1 색은 단축키와 우클릭 메뉴 둘 다로 칠합니다

- **음영** — Ctrl+Shift+1 ~ Ctrl+Shift+0
- **글자색** — Alt+Shift+1 ~ Alt+Shift+0

숫자 키 1~9가 팔레트의 1~9번이고 0이 열 번째입니다. 드래그로 여러 칸을 고른 뒤 누르면 한꺼번에 칠해집니다. 우클릭 메뉴에도 같은 색을 작은 사각형 스무 개로 늘어놓아 번호를 외우지 않고도 고를 수 있게 합니다.

Alt+Shift는 윈도우에서 입력 언어를 바꾸는 기본 단축키이기도 합니다. 숫자까지 눌러 Alt+Shift+3이 되면 언어는 바뀌지 않지만, 숫자를 놓쳐 Alt+Shift만 눌렀다 떼면 한/영이 바뀝니다. 이 일이 잦으면 글자색 쪽을 Ctrl+Alt+숫자로 옮깁니다.

### 15.2 팔레트는 순색 열 가지이고 음영과 글자색이 몫을 나눠 씁니다

검·흰·회1·회2에 빛의 삼원색(빨강·초록·파랑)과 그 보색(청록·자홍·노랑)을 더한 열 가지입니다. 파스텔이 아니라 순색이라 멀리서도 어느 칸이 어느 갈래인지 바로 보입니다.

같은 이름이 두 몫을 맡되 값이 다릅니다. **음영은 순색을 그대로 칠하고, 글자색은 같은 색상의 밝기만 내려서 씁니다.** 순색을 글자에 그대로 쓰면 흰 바탕에서 노랑은 1.07 : 1, 청록은 1.25 : 1, 초록은 1.37 : 1로 사실상 보이지 않기 때문입니다. 밝기를 내릴 때 세 채널을 같은 비율로 줄이므로 색상과 채도는 순색 그대로 남습니다. 파랑은 순색이 이미 어두워 `#0000FF`를 그대로 씁니다.

| 키 | 이름 | 음영 | 글자색 | 자동 글자 | 그 대비 |
|---|---|---|---|---|---|
| 1 | 검 | `#000000` | `#000000` | 흰색 | 21.00 : 1 |
| 2 | 흰 | `#FFFFFF` | `#FFFFFF` | 검정 | 21.00 : 1 |
| 3 | 회1 | `#C0C0C0` | `#737373` | 검정 | 11.54 : 1 |
| 4 | 회2 | `#808080` | `#3C3C3C` | 검정 | 5.32 : 1 |
| 5 | 빨강 | `#FF0000` | `#AE0000` | 검정 | 5.25 : 1 |
| 6 | 초록 | `#00FF00` | `#006300` | 검정 | 15.30 : 1 |
| 7 | 파랑 | `#0000FF` | `#0000FF` | 흰색 | 8.59 : 1 |
| 8 | 청록 | `#00FFFF` | `#005F5F` | 검정 | 16.75 : 1 |
| 9 | 자홍 | `#FF00FF` | `#980098` | 검정 | 6.70 : 1 |
| 0 | 노랑 | `#FFFF00` | `#585800` | 검정 | 19.56 : 1 |

맨 오른쪽 두 열은 다음 규칙의 결과입니다.

### 15.3 글자색을 고르지 않은 칸은 음영에 따라 검정과 흰색으로 뒤집힙니다

칸에 음영만 칠하고 글자색을 고르지 않았으면, 그 음영에서 대비가 높은 쪽으로 글자를 저절로 맞춥니다. 검정 음영과 파랑 음영에서만 흰 글자가 되고 나머지 여덟은 검정 글자입니다. 이 규칙 덕분에 **열 가지 가운데 무엇을 칠해도 값이 읽힙니다** — 가장 낮은 짝이 빨강 음영에 검정 글자를 얹은 5.25 : 1입니다.

글자색을 직접 고른 칸은 고른 색을 그대로 씁니다. 색 음영에 색 글자를 겹쳐 칠하면 100가지 짝 가운데 일부는 대비가 4.5 : 1에 못 미치는데, 직접 고른 것이므로 막지 않습니다.

### 15.4 숫자 서식은 우클릭 메뉴에서 지정합니다

우클릭 메뉴의 **숫자 서식…** 한 줄이 작은 입력창을 띄웁니다. 자주 쓰는 여섯 개(일반, `0`, `0.00`, `#,##0`, `0.00%`, `yyyy-mm-dd`)를 목록으로 두고 맨 아래에 서식 코드를 직접 치는 칸을 둡니다. 해석하는 서식 코드의 범위는 4장에 있습니다.

### 15.5 서식 지우기는 Ctrl+Shift+Space입니다

음영과 글자색과 숫자 서식을 한 번에 기본 상태로 되돌립니다 — 흰 바탕, 검정 글자, 일반 서식입니다. 우클릭 메뉴에도 **서식 지우기**를 둡니다. 값과 수식은 지우지 않습니다(값을 지우는 것은 Delete입니다).

---

## 16. 성능 목표

아래 수치를 맞추는 것을 목표로 삼습니다. 다 만든 뒤에 이 표로 확인합니다.

| 항목 | 목표 | 근거 |
|---|---|---|
| 키를 누르고 글자가 보이기까지 | 16.7ms | 60Hz 화면의 한 프레임 = 1/60초 |
| 트레이 클릭에서 창이 뜨기까지 | 100ms | 사람이 "즉시"로 느끼는 경계 |
| 프로그램 시작 | 300ms | 로그온할 때 자동으로 뜨므로 부팅을 늦추면 안 됨 |
| 시트 한 장(5,200칸) 전체 재계산 | 10ms | 한 프레임 안에 끝나야 타이핑이 끊기지 않음 |

이 수치를 맞추려고 정한 것이 세 가지 있습니다. 그리드는 칸마다 컨트롤을 만들지 않고 화면에 보이는 100칸(5열 x 20행)만 직접 그립니다. 재계산은 의존 그래프를 따라 바뀐 칸에 달린 것만 셉니다(8.6). 저장은 배경 스레드에서 합니다(13.2).

측정 코드는 개발 중에만 켜는 스위치 뒤에 둡니다. 늘 띄워 두는 프로그램에 측정 장치가 상주하면 그 자체가 목표를 깎습니다.

---

## 17. 기술 스택과 배포

### 17.1 C# + WPF(.NET 8)로 만듭니다

프레임리스 창, 트레이 아이콘, 작업 표시줄 숨김(WS_EX_TOOLWINDOW)이 전부 윈도우 API 호출 한두 줄이고, 그리드를 DrawingContext로 직접 그리면 16.7ms 목표를 맞출 수 있습니다.

다른 후보는 시작 시간에서 걸립니다. Electron은 시작이 1초대라 300ms 목표의 세 배를 넘고, 파이썬을 묶은 실행 파일도 1초 안팎입니다. Tauri는 가볍지만 WebView2에 기대야 하고 한글 IME 처리가 변수로 남습니다.

### 17.2 런타임을 품은 단일 실행 파일로 내보냅니다

.NET 런타임을 따로 설치하지 않아도 되도록 런타임을 실행 파일 안에 넣습니다. 크기가 5MB에서 70MB 안팎으로 늘지만, 설치 권한이 없는 PC에도 파일 하나만 복사하면 바로 돌아갑니다. 설치 프로그램은 만들지 않습니다.

### 17.3 코드 서명은 하지 않습니다

서명하지 않은 실행 파일은 윈도우가 처음 실행할 때 "PC를 보호했습니다" 창을 띄웁니다. **추가 정보**를 누르고 **실행**을 고르면 그다음부터는 뜨지 않습니다. 서명서 값(연 10만 원대부터)을 개인이 쓰는 프로그램에 들일 이유가 없다고 보아 이 경고를 감수합니다.

---

---

## 부록 A. 여덟 항목이 모두 확정되었습니다

여덟 항목이 모두 정해져 본문의 장으로 들어갔습니다. 아래는 어느 항목이 어느 장이 되었는지 적은 것이며, 번호는 대화에서 쓰던 고정 번호입니다.

| 번호 | 항목 | 어디에 있는가 |
|---|---|---|
| A-1 | 수식 문법 | 8장 |
| A-2 | 복사·붙여넣기 | 10장 |
| A-3 | 그리드 크기 | 12장 |
| A-4 | 자동 저장 | 13장 |
| A-5 | 창 조작 | 14장 |
| A-6 | 색과 서식 적용 | 15장 |
| A-7 | 성능 목표 | 16장 |
| A-8 | 기술 스택 | 17장 |

---

## 부록 B. 범위 밖으로 두는 기능

명세에 없어 넣지 않는 것들입니다. 이 목록은 아직 확인을 받지 못했습니다 — 넣어야 할 것이 섞여 있으면 부록 A로 옮깁니다. 나중에 넣으려면 저장 구조와 수식 참조를 함께 고쳐야 하는 항목이 있어 미리 적어 둡니다.

- 인쇄, 차트
- 정렬, 필터
- 셀 병합
- 조건부 서식
- 찾기·바꾸기
- 셀 주석
- 행·열 삽입·삭제(넣으면 기존 수식의 참조를 모두 옮겨야 함)
