<Window x:Class="DesktopSheet.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:DesktopSheet.App"
        Title="DesktopSheet"
        Width="560" Height="444" MinWidth="352" MinHeight="144"
        WindowStyle="None" ResizeMode="CanResize" ShowInTaskbar="False"
        Background="White" UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">

    <!-- 12.4: 프레임리스라 테두리가 없으므로 가장자리 6px 을 크기 조절로 잡히는 자리로 둔다 -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6"
                            GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <!-- 테두리가 없으면 창이 바탕과 붙어 보여 얇은 선을 하나 두른다 -->
    <Border BorderBrush="#8D96A3" BorderThickness="1" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 12.2: 탭은 위쪽에 붙이고 오른쪽 빈 자리가 창 끌기 영역을 겸한다 -->
        <Border x:Name="TabBar" Grid.Row="0" Background="#EFF1F4" BorderBrush="#D0D4DA" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TabBar_MouseLeftButtonDown">
            <StackPanel x:Name="TabPanel" Orientation="Horizontal" />
        </Border>

        <Grid Grid.Row="1">
            <local:SheetGrid x:Name="GridView" />
            <!-- 10.2: 늘 커서 칸 위에 있고 포커스를 쥔다. 값을 치기 전에는 보이지 않는다. -->
            <TextBox x:Name="Editor"
                     HorizontalAlignment="Left" VerticalAlignment="Top"
                     BorderBrush="#0B48C8" BorderThickness="0" Padding="6,0,6,0"
                     Background="Transparent" Foreground="Transparent" CaretBrush="Transparent"
                     IsHitTestVisible="False"
                     FontFamily="Nanum Gothic Coding, D2Coding, Consolas" FontSize="16"
                     VerticalContentAlignment="Center" />
        </Grid>
    </Grid>
    </Border>
</Window>
