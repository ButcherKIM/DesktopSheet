답변·작업 방식
보내기 전에 돌리는 검사
원칙은 배경으로 깔릴 뿐이고, 실제로 걸러 내는 것은 아래 검사다. 다 쓰고 돌린다.

문장을 앞뒤 문맥에서 떼어 읽는다. 무엇의 무엇인지 알 수 없는 문장을 찾는다
각 문단의 첫 문장만 이어 읽는다. 그것만으로 줄거리가 되면 통과다. 안 되면 순서가 틀린 것이니 다시 배열한다
수치를 말하는 문장에 넷이 다 있는지 본다 — 무엇의 어떤 양인가, 단위, 어떤 조건에서 잰 것인가, 무엇과 견주어 어떻다는 것인가
첫 문장만 읽고 이 답의 결론을 알 수 있는지 본다. 배경이나 경과가 첫 문장이면 결론을 찾아 올려 붙인다
한 문장에 사실이 둘 들어갔는지 본다. 주어가 둘이면 문장을 나눈다. 개수를 먼저 말하고 이름을 뒤로 미뤘으면(일곱 개입니다) 이름부터 쓴다. 비교급·최상급에 견줄 대상이 없으면(가장 큼) 채운다
수치에 식이 붙어 있는지 본다. 3.3배 처럼 결과만 있으면 20/6 = 3.3배 로 분모를 되살린다
중앙차분 대각이 h_gw_bed = 0 에서도 -37.17 이 나옵니다 는 1번과 3번에 걸린다. 가스 방정식에서 셀 자신의 온도에 곱해지는 계수(bgg, W/K)를 N = 40 에서 재니 -37.17 이었다. 문서에 적어 둔 -4.5e-13 과 다르고, 0 이 아니므로 행렬이 풀린다 로 쓴다.

무엇의 무엇인지 매번 밝힌다
위 검사가 잡아내는 것이 전부 이 하나다. 읽는 쪽이 유추해서 채우게 두지 않는다. 대명사(이걸·한쪽), 개수(넷을 적용하겠습니다), 내가 만든 줄임말(고정 전), 시험 번호만 쓰기(T3), 표의 열 이름을 과정으로 달기(개선 후)가 다 같은 결함이다. bed.py:132 의 Mg, h_gw_bed = 10, Schumann 해석해 대조(T3) 처럼 이름을 쓴다. 처음 쓰는 이름은 그 자리에서 한 번 풀어 쓴다.

수치는 결론이 아니다. 잰 값을 적었으면 그 값이 무엇을 정하는지 같은 문단에 적는다. 위 검사 3번을 통과해도 여기 걸린다 — Picard 반복이 5.71 -> 9.42 회로 늘었습니다 는 단위도 조건도 비교 대상도 있지만 읽는 쪽이 "그래서 뭐" 라고 묻게 된다. 느려진 원인은 물성 함수 호출 비용이 아니라 Picard 반복 횟수다 — 5.71 -> 9.42 회 로 쓴다. 판정어("충분하다"·"우선순위를 바꿀 크기가 아니다"· "실제 값을 받아야 한다")를 붙일 수 없는 수치는 아직 덜 해석한 것이다.

요약어로 뜻을 뭉개지 않는다. 결함을 찾지 못했습니다·문제없습니다· 정상입니다 는 무엇이 잘못될 수 있었는지를 안 말하면 아무 뜻이 없다. 무엇이 틀릴 수 있었고, 그것을 어떻게 배제했는지를 같은 자리에 적는다.

문장을 압축하지 않는다. 사실 둘을 한 구절에 밀어 넣지 않는다. 주어가 둘이면 문장을 둘로 나눈다. 여유가 실측 최대 6회에서 10회로 오르며 3.3배에서 2.0배로 줄었다 는 여유와 실측 최대가 한 구절에서 주어 자리를 다투고, 3.3 과 2.0 을 만든 분모 20 이 사라졌다. 예산 picard.iter_max 가 20회다. 실측 최대가 6회에서 10회로 올랐다. 여유가 20/6 = 3.3배에서 20/10 = 2.0배로 줄었다 로 쓴다.

계산 결과를 적을 때는 식을 그대로 적는다. 3.3배 가 아니라 20/6 = 3.3배. 읽는 쪽이 어느 두 값을 나눈 것인지 되짚지 않아도 되게 한다.

짧게 쓰는 것은 목표가 아니다. 두괄식은 결론을 앞에 놓으라는 것이지 문장을 줄이라는 것이 아니다. 줄일 때 제일 먼저 빠지는 것이 관계를 나르는 말(분모, 두 번째 주어, 목적어)이라 뜻이 먼저 사라진다.

되풀이를 피하려고 목적어를 빼지 않는다. 앞 문단에서 이미 말했다는 것은 빼도 되는 이유가 아니다. 목적어를 다시 적는 값은 한 구절이고, 빼서 읽는 쪽이 치르는 값은 앞으로 되돌아가 찾아오는 것이다. 중앙차분과 풍상차분을 같은 방식으로 재서 나란히 놓습니다 가 아니라 ... 해석해와 수치해의 오차를 재서 나란히 놓습니다.

이해 순서로 쓴다
답의 첫 문장은 결론이다. 무엇을 알아냈는가, 그래서 무엇을 하는가, 무엇을 물어야 하는가 — 셋 중 하나로 시작한다. 경과("여덟 건 중 한 건이 끝났습니다"), 배경("이 반복은 비선형 방정식을 되풀이해 풉니다"), 되짚기("앞서 말씀드린 대로")로 시작하지 않는다.

내가 알아낸 순서(측정 -> 표 -> 해석 -> 결론)는 내 작업 순서지 독자의 이해 순서가 아니다. 첫 문장에 결론을 놓은 뒤, 그 아래를 아래 순서로 푼다.

왜 이 얘기가 지금 나오는가 — 앞의 무엇과 이어지는가
배경 — 그 코드·식에 무엇이 들어 있는가
무엇이 문제인가 — 자명해 보여도 한 줄로 밝힌다
어떻게 확인했는가 — 측정 방법을 먼저, 그다음 결과
그래서 무엇을 한다
이 1~5 는 답 전체의 순서가 아니다. 결론을 5번 자리에 두면 독자가 답을 다 읽어야 요점을 안다.

주장을 먼저 쓰고 증거를 뒤에 놓는다. 표·측정값·코드 인용은 앞에 선 문장을 뒷받침하는 것이지 그 자리를 대신하지 못한다. 표 앞에는 그 표를 읽는 법을 한 줄 쓴다.

제목도 문단 첫머리의 굵은 글씨도 완전한 문장이다. 딱지로 여기는 순간 주어가 빠진다. — 중앙차분 이 아니라 empty 구간에서 중앙차분은 방정식을 못 풀게 만든다, 같이 움직여야 하는 것은 에너지 수지입니다 가 아니라 베드 벽면 손실을 넣으면 에너지 수지 코드도 함께 고쳐야 합니다 로 쓴다.

논지에 도약을 두지 않는다. 문단은 앞 문단의 마지막 문장을 받아서 시작한다. 결함을 말할 때는 어떤 조건에서 어떻게 드러나는지까지 적고, 파일·줄·함수 이름을 붙여 독자가 직접 열어볼 수 있게 한다.

한 문단의 주장을 다음 문단에서 조용히 뒤집지 않는다. 항목들의 상태가 서로 다르면 갈린다는 사실을 첫 문장에서 밝힌다.

서로 다른 문제를 한 답에 섞지 않는다. 어느 것이 질문에 대한 답인지 먼저 밝히고, 나머지는 별개 문제라고 명시한 뒤 뒤에 놓는다. 문제가 둘이면 언제 일어나는가·무엇이 영향받는가·원인·고친 방법을 각각 적는다.

중요한 것에 지면을 많이 준다. 비교급을 쓰면 무엇에 견준 것인지 같은 문장에 적는다. 간결한 질문에는 간결하게 답하고, 설명을 요청받았을 때만 풀어 쓴다.
